/*class a {
      protected double balance;
      private a(float amount){
          if(amount>balance){
              balance=amount;
          }
      }
}
class currentAccount extends a{
    final static double overDraftLimit=-10000;

    public boolean
}
class SavingAccount extends a{
    float FD[];
    double transaction;
    final static float   transaction_limit=50000.0F;
    public SavingAccount(float amount){
        super(amount,true);
        FD =new float[10];
    }
public boolean withDraw(float amount){
        if(transaction>transaction_limit){
           return false;}
        else {
            if (super.withDraw()) ;{
               transaction += amount;}
        }
}
}*/
